# TODO

Ordered by priority.

1. Ensure gmail-ingest --version works and document where/how to maintain the version number

2. Ensure this project can also run on Linux: try in a container

3. Add a 'schedule' command to the CLI that actually does what register_task.ps1 does now
   Make sure it also works with cron when running on Linux

4. Review README.md and ensure it fully documents this project's actual state.
   Also try to condense the information more and use 132 line-length in general.

5. Rename the current project and also setup a GitHub project named 'mail-utils'
